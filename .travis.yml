#language: cpp
#language: objective-c

matrix:
  include:
  - os: osx
    language: cpp
    compiler: clang
  - os: osx
    language: objective-c

#compiler: clang

install:
  - rvm use 2.1
  - gem install xcpretty

script:
  - echo hola
