language: nodejs
os: osx
osx_image: xcode7

before_install:
  - brew install nvm


before_script:
  - nvm install 0.12
  - nvm install 0.10

script:
  - uname -a
  - env
  - nvm use 0.10
  - node -v
  - nvm use 0.12
  - node -v
  - echo "done done done"

